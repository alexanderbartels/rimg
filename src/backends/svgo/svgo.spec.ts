
import { expect } from 'chai';
import 'mocha';

import {SvgoBackend} from '.';
import {SvgoCompressionExecutor} from './SvgoCompressionExecutor';

describe('svgo backend', () => {
    it('should have tests', () => {
        // TODO provide some tests

        /* tslint:disable:no-unused-expression */
        expect(SvgoBackend).to.be.not.undefined;
        expect(SvgoCompressionExecutor).to.be.not.undefined;
        /* tslint:enable:no-unused-expression */
    });
});
